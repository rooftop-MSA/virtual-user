sonar {
    properties {
        property "sonar.projectKey", "rooftop-MSA_virtual-user"
        property "sonar.organization", "rooftop-msa"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}
